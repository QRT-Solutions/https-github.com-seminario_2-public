# QRT Solutions: this edition requires no environment variables or credentials.
# Select the project explicitly with seminario --project PATH when outside its tree.
# No automatic .env loading or external-service connections are configured.
